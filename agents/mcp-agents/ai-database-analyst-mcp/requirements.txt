# ===== AI Database Analyst MCP Agent — runtime dependencies =====
# Loose lower-bound pins (no upper caps) so pip can resolve pre-built wheels
# for whatever Python the host runs (tested on 3.11–3.13, incl. Streamlit Cloud).

# --- Web UI ---
streamlit>=1.39

# --- AI / LLM ---
google-generativeai>=0.8.3

# --- Database / ORM ---
SQLAlchemy>=2.0.30

# --- Database drivers (only used for the matching DB_TYPE) ---
PyMySQL>=1.1            # MySQL / MariaDB
psycopg2-binary>=2.9.9 # PostgreSQL
duckdb>=1.1            # DuckDB
duckdb-engine>=0.13    # DuckDB SQLAlchemy dialect

# --- Data / Analysis ---
pandas>=2.2

# --- Visualisation ---
plotly>=5.24

# --- Validation / Config ---
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# --- Exports ---
openpyxl>=3.1
XlsxWriter>=3.2
reportlab>=4.2
tabulate>=0.9

# --- SQL parsing / utilities ---
sqlparse>=0.5
tenacity>=9.0

# NOTE: The Model Context Protocol layer in mcp/ is implemented natively in this
# project, so the third-party `mcp` SDK is intentionally NOT a dependency.
