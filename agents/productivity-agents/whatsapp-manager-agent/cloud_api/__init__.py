"""Official WhatsApp Business Cloud API route (archived alternative to the QR bot).

This is the ToS-compliant path via Meta. Not used by the live QR-bot deployment.
Run with:  uvicorn cloud_api.webhook:app --host 0.0.0.0 --port 8000  (from project root)
See DEVELOPMENT.md.
"""
